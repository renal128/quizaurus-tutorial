# Quizaurus Tutorial Repo

This repo contains two ChatGPT demo apps for educational purposes:

 - `quizaurus-plain` is a simple quiz app where the frontend part is implemented using plain JavaScript

 - `quizaurus-react` is pretty much the same app, but the frontend uses React, and it has a couple more bells and whistles.
